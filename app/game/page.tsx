import React from "react";

import { GameWindow } from "@/components/game";

const Game = () => {
    return (
        <section className="pb-16">
            <GameWindow />
        </section>
    );
};

export default Game;
